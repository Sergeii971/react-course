import React from 'react';
import logo from '../../components/woolmarkimagelogo-1024x576.png';

const Logo: React.FC = () => (
	<img src={logo} alt='Image' width={60} height={30} />
);

export default Logo;
